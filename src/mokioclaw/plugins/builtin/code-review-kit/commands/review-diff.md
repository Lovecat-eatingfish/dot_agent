Review the current git diff (or staged changes if present).

Focus on:
- regressions and missing tests
- security-sensitive paths
- unclear naming

Return a concise bullet list of findings with severity tags.
